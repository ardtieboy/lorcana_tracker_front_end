export type CardObject = {
    name: string;
    id: string;
    image: string;
};

export type SetObject = {
    set_name: string;
    set_id: string;
    set_num: number;
};

export type SelectionType = {
    name: string;
    id: number;
};

